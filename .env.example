# DeepSeek 和其他兼容 OpenAI Chat Completions 的服务均可使用。
# 不要把真实密钥提交到代码仓库；本地写入 .env.local，部署时写入 Vercel 环境变量。
LLM_API_BASE_URL=https://api.deepseek.com
LLM_API_KEY=
LLM_MODEL=deepseek-v4-flash
# 可选：主模型返回 429/5xx 时使用的备用模型。
LLM_FALLBACK_MODEL=
